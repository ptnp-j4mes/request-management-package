# Static workspace modules

These files were copied from the uploaded `workspace-platform-shadcn` zip and are loaded by `WorkspacePlatform.tsx` inside an iframe.

They remain static prototype modules for visual parity. Live repo routes can be opened from the workspace header using `Open live route`.
